<div class="container m-5">
    <h1 class="display-4 m-5" style="letter-spacing: 5px;">Technology News</h3>

        {{#each articles}}
        <a class="link-dark text-decoration-none" href="/api/article/tech/{{ title }}">
            <!--Article container-->
            <div class="row mt-5 mb-5">

                <div class="column-left col-md-7 p-4">
                    <img class="img-fluid" src="{{ urlToImage }}" alt="article-picture">
                </div>
                <div class="column-right col-md-5 m-auto">
                    <h2 class="Display-6">{{ title }}</h2>
                    <span>Author: {{ author }}</span>
                    <p class="lead">{{ description }}</p>
                </div>
            </div>
            <!--End article container-->
        </a>

        {{/each }}
</div>